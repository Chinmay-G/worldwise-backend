{
  "name": "worldwise-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "son-server --host 0.0.0.0 --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
